#text(weight: "bold")[Условие]

Существенное ускорение без использования потоков до 2 баллов
(требуется разобраться в коде) (для данного пункта требуется выполнить замеры без параллелизма и занести в отчет результат до и после).

#text(weight: "bold")[Решение]: 

я изменил get в vectorfield, результаты 

до
#figure(
  image("before.png")
)

#figure(
  image("brefore2.png")
)

после
#figure(
  image("after.png")
)

#figure(
  image("after2.png")
)

Получилось ускорить программу почти в 3 раза :)))